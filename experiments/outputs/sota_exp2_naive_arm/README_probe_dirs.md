# Probe Directory Names

This folder contains two preserved probe snapshots for the Exp2 naive arm:

- `probe_current/`: the current canonical probe snapshot for this arm.
- `probe_legacy_earlier_run/`: an earlier probe snapshot kept for provenance.

The production null-arm runs are in the sibling `*_run01..05` files/directories,
and the merged canonical summary is `summary.json`.