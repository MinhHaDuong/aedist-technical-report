%erg 0.1
Title: Quality composite axis: coherence/provenance/temporality scorers
Created: 2026-05-21
Author: claude

--- log ---
2026-05-21T12:30Z claude created — follow-up from ticket 0196 (Pareto figure)
2026-06-08T11:00Z claude note linked to tracker 0464
2026-06-08T19:15Z HDMX-coding-agent note scope conflict — cannot implement safely without author guidance. Three-way conflict: (1) original exit criteria specify per-run scalars in records_to_metrics() — but 0396 precedent explicitly says coherence metrics do NOT go in measurements.jsonl; (2) 0453 body item #4 says 0201 should consume 0453's variability scorers and own the composite aggregation — but 0464 tracker assigns aggregation to 0453 (run-grain veto + model-grain floor); (3) 0454 (status-vocabulary alignment, open/imagine) is an unresolved dependency for the status-correct sub-score. Safe partial: score_exp1.py is missing coherence_status_vocab_adherence + coherence_row_atomicity columns (already in score_mechanical.py / sota_cross_eval.csv); adding parity does not touch contested aggregation. Question for author: (a) does 0201 own the composite/floor aggregation (per 0453 body #4), or only the scorer components with floor logic living in 0453 (per 0464 tracker)? (b) is the status-correct sub-score deferred until 0454 resolves status vocabulary?
2026-06-09T00:00Z HDMX-coding-agent note split — child 0488 created for the minimum wiring (Exp1 mechanical coherence columns → exp1_cross_eval.csv) needed to unblock 0487 (Haiku heatmap false pass). 0201 retains full scope: composite aggregation, provenance, temporality (post-preprint, contested questions unresolved).
2026-06-11T09:30Z HDMX-coding-agent note boundary with 0397 (now 0464 child A8): 0201 owns the per-field scorer components; 0397 consumes them and owns the within-row internal/external pass-fail composites plus the external checks (province gazetteer, ≥30 MWe scope). 0397's within-row AND is orthogonal to the contested run-grain veto / model-grain floor question above (which aggregates across rows/runs).

--- body ---

## Context

The Figure 2 Pareto chart (ticket 0196) plots Accuracy (row-level F1)
on the y-axis. The §2 quality framework defines four dimensions:
Accuracy, Coherence, Provenance, Temporality. Today only Accuracy is
scored — the other three lack measurement infrastructure.

Until those scorers exist, the Pareto figure axis must stay narrow
("Accuracy" not "Quality") to avoid overclaiming. This ticket tracks
the work to extend it.

## Blocked-by

Blocked-by Experiments 2/3 data tickets — coherence/provenance/temporality
metrics are only meaningful when the dataset includes the rich-output
variants (RAG, decomposed, agentic) that Exp 1 deliberately excludes.

## Exit criteria

- [ ] Three scorers landed (coherence, provenance, temporality) with
  unit tests on a small reference dataset.
- [ ] Each scorer exposes a per-run scalar in `records_to_metrics()`
  per ADR-7.
- [ ] A composite Quality score is defined (mean, weighted mean, or
  worst-of) with the choice justified in the docstring.
- [ ] Pareto figure y-axis can be flipped between Accuracy and the
  composite via a `--quality-axis` flag.
- [ ] Manuscript Figure 2 caption updated to reflect whichever axis
  ends up canonical.

Part of: 0464 — role A1 (deterministic reference-free scorer; 4 free criteria + source quality resolve→tier→value-in-source; accuracy is the only reference-full axis)
