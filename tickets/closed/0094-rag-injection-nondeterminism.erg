%erg 0.1
Title: Diagnose non-deterministic RAG document injection in worker
Created: 2026-04-16
Author: claude
Closed: migrated from Status: closed

--- log ---
2026-04-16T15:30Z claude created — found during PR #250 temperature investigation
2026-04-16T16:00Z claude claimed
2026-04-16T16:30Z claude status closed — root cause identified

--- body ---
## Root cause

Experiment straddled ticket 0084 (PR #244) code boundary. Before 0084,
worker.py did NOT call build_api_kwargs — requests went to OpenRouter without
temperature or tools kwargs. After 0084, worker.py calls
`build_api_kwargs(model_entry, temperature=0.0)` which adds both
`temperature=0.0` AND `tools=[openrouter:web_search]`.

The ~300K extra tokens in Batch 2 are web search results injected by
OpenRouter when the web_search tool is enabled. In 16/20 DeepSeek runs,
the model requests MORE searches (finish_reason=tool_calls) instead of
extracting from the RAG corpus. 4 runs succeed despite over-context.

Evidence: (1) git show cba5f19 (PR #244) adds `build_api_kwargs` import to
worker.py; (2) Batch 1 (21:20-21:22, no temperature key): pre-0084 code;
(3) Batch 2 (21:22-21:23, temperature=0.0): post-0084 code; (4) all 64 runs
have strategy=wholesale and corpus_files; (5) token diff is web search
injection, not RAG. Fix: ticket 0095 should re-run with web_search disabled
for RAG mode (RAG corpus IS the context, web search is counterproductive).


## Context

PR #250 (Phase 2 ablation) shows that replicates of the same prompt
variant receive wildly different token counts: ~72K (no RAG documents)
vs ~350K (RAG documents injected). The two replicates are supposed to
be identical except for random seed.

Evidence from measurements.jsonl:
- DeepSeek p2_sourcing run1: 72,516 tokens (t=null, ok)
- DeepSeek p2_sourcing run2: 353,691 tokens (t=0.0, refusal)
- Kimi p2_composite run1: 282,248 tokens (over-context, ok)
- Kimi p2_composite run2: 75,911 tokens (normal)

Temperature also varies across replicates (null vs 0.0), suggesting
runs straddled the ticket 0084 enforcement boundary.

## Actions

1. Read the worker dispatch code (src/aedist/worker.py, query scripts)
   to trace how RAG documents are loaded and injected into prompts.
2. Check whether RAG injection depends on a flag that changed between
   the two run batches (e.g., a config reload after PR #244 merge).
3. Determine whether the non-determinism is a race condition, a config
   boundary, or an intentional fallback when documents are unavailable.
4. Document the root cause.

## Test

Reproduce: run the same sweep config twice and verify both replicates
get identical token counts (within tokenizer variance).

## Exit criteria

- Root cause identified and documented in ticket log
- Fix proposed (may be a separate ticket if non-trivial)
