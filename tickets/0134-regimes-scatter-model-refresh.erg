%erg v1
Title: Rafraîchir les modèles cloud du regimes scatter — GPT-5.5, 70B cheap
Status: open
Created: 2026-04-29
Author: claude

--- log ---
2026-04-29T19:00Z claude created
2026-04-29T20:00Z claude note restreint aux modèles cloud (GPT-5.5 + 70B cheap) — local 30B→0136, 8B→0137

2026-04-29T21:06Z claude note sweep-assess: not-picked hash:eef466fab30c scope:add GPT-5.5 and 70B cheap cloud models to regimes scatter risk:medium
--- body ---
## Context

`fig_regimes_scatter.pdf` affiche actuellement 4 modèles : GPT-5.4, Mistral S4,
Mistral L3, DeepSeek. Ce ticket ajoute les modèles cloud manquants.

## Modèles candidats et gaps

| Rôle | Slug candidat | Méthodes manquantes |
|---|---|---|
| GPT-5.5 | `openai/gpt-5.5` (à confirmer sur OpenRouter) | toutes (nouveau modèle) |
| 70B cheap distant | `qwen/qwen3.5-122b-a10b` | `direct+multiturn` |

## Actions

1. Confirmer le slug OpenRouter pour GPT-5.5.
2. Ajouter les sweeps manquants dans `experiments/experiments.toml` (repeat=3, seed=42).
3. Lancer les sweeps ; vérifier la qualité des sorties.
4. Mettre à jour `_MODELS` dans `plot_regimes_scatter.py`.
5. Regénérer `fig_regimes_scatter.pdf` et committer.

## Exit criteria

- `fig_regimes_scatter.pdf` inclut GPT-5.5 et un distant 70B cheap
- Chacun a les 3 méthodes dans measurements.jsonl
- `tectonic slides.tex` sans erreur
## Picker assessment 2026-04-29T21:06Z
**Decision:** not-picked
**Scope:** add GPT-5.5 and 70B cheap cloud models to regimes scatter
**Risk:** medium
**Reason:** involves real API spending on OpenRouter
